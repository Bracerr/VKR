# Устарело: gateway входит в make prod-up (sed-service/e2e_tests/docker-compose.prod.yml).
# Этот файл оставлен для справки; отдельно не запускайте.

services:
  gateway:
    image: nginx:1.26-alpine
    ports:
      - "5656:5656"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
