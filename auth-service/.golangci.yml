run:
  timeout: 5m
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gofmt
    - misspell
linters-settings:
  errcheck:
    check-blank: true
issues:
  exclude-rules:
    - path: internal/mocks/
      linters:
        - all
