# Для Docker из корня VKR: секреты в `VKR/.env` (см. `VKR/.env.example`). Ниже — пример для локального `go run`.

# Только секреты / строки подключения (остальное — в configs/*.yaml)
DB_DSN=postgres://wh:wh@localhost:5433/warehouse?sslmode=disable
